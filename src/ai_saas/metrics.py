def conversion(converted,total): return 0 if total==0 else converted/total
def retention(active_returning,cohort): return 0 if cohort==0 else active_returning/cohort
def ai_cost_per_active_user(total_ai_cost,active_users): return 0 if active_users==0 else total_ai_cost/active_users
